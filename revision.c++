// static variable in class -->>> variable class 
// Static in method -->>> u can use it without any object
// static function must be with static vaiable
// any global variables intialize = 0 but local is rubbish value
// if u use shallow the past data will be removed if the new is removed so in this case we will need to define a deep not shallow
// changing the pointer position depend on const variable is with int or with pointer